<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.natsufumij.blacksky.mapper.PostMapper">
    <select id="fetchPost" resultType="com.natsufumij.blacksky.domain.Post">
        select
        id,user_id,create_time,content,reply_count,reply_post_id
        from post where id=#{id}
    </select>

    <select id="fetchPostsByUserId" resultType="com.natsufumij.blacksky.domain.Post">
        select
        id,user_id,create_time,content,reply_count,reply_post_id
        from post where user_id=#{userId}
    </select>

    <select id="fetchPostsReplyOn" resultType="com.natsufumij.blacksky.domain.Post">
        select
        id,user_id,create_time,content,reply_count,reply_post_id
        from post where reply_post_id=#{postId}
    </select>

    <insert id="post" useGeneratedKeys="true" keyProperty="id">
        insert into post(user_id,content,reply_post_id)
        values(#{userId},#{content},#{replyPostId})
    </insert>

    <delete id="deletePost">
        delete from post
        where id=#{postId}
    </delete>

    <update id="countAdd">
        update post
        set reply_count=reply_count+#{count}
        where id=#{postId}
    </update>
    
    <update id="countDec">
        update post
        set reply_count=reply_count-#{count}
        where id=#{postId}
    </update>
</mapper>